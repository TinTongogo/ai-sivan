package com.icusu.sivan.common.enums;

/** 技能状态：ACTIVE = 可用，INACTIVE = 停用，OBSERVING = 观察中（统计使用表现），ARCHIVED = 已归档（不可用）。 */
public enum SkillStatus {
    ACTIVE,
    INACTIVE,
    OBSERVING,
    ARCHIVED
}
